# Gemfile
source "https://rubygems.org"

# Use the GitHub Pages gem bundle (pins Jekyll and plugins to GH Pages versions)
gem "github-pages", group: :jekyll_plugins

# Needed for Ruby 3+ local serving
gem "webrick", "~> 1.8"
